export type DataProps = {
    name: string
    area: string
    row: string
    seat: string
    image: string
    date: string
    membership: string
    entry?: string
    code: string
    qr: string
}